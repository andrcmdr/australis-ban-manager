- Track successful transactions, will help with decision if the rules are
imbalanced.
- Implement gas increment when the fields are added to the relayer.
- Tests
- Refactor everything
- Save periodically as JSON
- Load data, if it exists
